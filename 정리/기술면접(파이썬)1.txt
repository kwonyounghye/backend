스트림 : 데이터의 논리적인 흐름
FIFO - First In First Out(ex. stream)
LIFO : Last In First Out(ex. stack)

DDL - 데이터 정의 언어(테이블, 뷰)
	create, drop
DML - 데이터 조작 언어(레코드)
	insert, update, delete
DQL - 데이터 질의 언어
	select, where
DCL(control)

인터프리터 : 소스 전체
컴파일러 : 한줄씩 실행

SQLite : 파일(소형DB)

NoSQL

정형데이터 - RDBMS

비정형데이터 - 

얕은 복사 - 참조변수만 복사
깊은 복사 - 참조변수 + 내용 복사

이진탐색: 

큐와 스택

GUI 프로그래밍
자바		장고
M	모델	M	모델
V 	뷰	T	템플릿
C	컨트롤러	V	뷰

평균값 정규분포
중앙값 비정규분포
최빈치 

정규화 : 스케일링

데이터 축소

과대적합: 학습이 너무 잘돼서 검증이 잘 안됨
과소적합: 너무 학습이 안돼서 잘 못맞춤

모집단과 표본: 모집단 전체 집단
전수조사: 전체 조사

데이터 수집 => 전처리 => 후보변수 => 유의한 변수 선택

Logit()
coef 기울기(상관계수)
p-value : 0.05 작으면 유의
다중공선성 : 

엔트로피와 지니 : 

활성화함수 : 입력신호의 총합을 출력신호로 바꿔줌

input 		hidden 	output
독립변수의 수		회귀(실제값) - 변경할 필요 없음
					activation = 'linear'(회귀) 항등함수
			이진분류(0,1)
					sigmoid 0.0~1.0
			다분류(0~9)
					softmax
					0일 확률, 1일 확률, ,,9일 확률

k-means, svm - 거리기반 모델

텍스트

신경망 input hidden output

딥러닝 hidden layer를 여러개로 늘림

손실함수
	회귀 mse/rmse/mae
	이진분류 binary_crossentropy
	다분류 categorical_crossentropy

tensor 다차원 데이터 배열

train/vaildation(가중치, 편향)/test => 신경망
6	2		2

CNN 

합성곱 : 필터를 뒤집고 각 자리마다 곱한 값을 연산
교차상관 : 필터를 뒤집지 않고 곱함
일반적으로는 왼쪽 노드나 오른쪽 노드가 참여율이 떻어짐(제로 패딩을 해서 보완)
cnn을 통과하면 값이 줄어듬(패딩을 넣어주면 학습이 잘 될 수 있다)
합성곱 계층 : 필터값은 랜덤으로 결정, 정보가 줄어들지 않게 패딩을 넣음
맥스풀링 : 각각의 영역에서 가장 큰 값을 뽑아냄
 - 이미지 축소 계산량 감소

패딩: 사이즈가 줄어들지 않도록 0 추가

로지스틱 회귀분석
	-오즈비

가중치 규제
l2 정규화 패널티를 크게 해서 학습이 더 잘 되게 함

온라인학습		배치학습		mini batch
샘플수 100개		샘플100개	샘플 100개, 10개씩
epoch 100		에폭1회
에폭 1회 - 샘플 100개
for epoch in range(1,101): 에폭 1~100
for row in 샘플리스트: